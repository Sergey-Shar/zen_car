import { memo } from "react";

const WorksType = () => {
  return (
    <div style={{ marginTop: "7rem", textAlign: "center", height: "20rem" }}>
      <h2>КАТАЛОГ УСЛУГ</h2>
    </div>
  );
};

export default memo(WorksType);
